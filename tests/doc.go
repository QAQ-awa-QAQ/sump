// Package tests 是 SUMP v2 的“总测试”：跨服务的端到端测试。
//
// 与各服务自带 tests/（服务内测试）的分工：
//
//   - 服务内测试：单服务行为——起真实服务、走真实 WS，但只涉及一个服务；
//   - 总测试（本包）：多个服务的真实进程 + 真实 WS 通信，
//     验证“服务隔离 + 网络互访”。用例从 M1.5 起逐步加入：
//     注册 / roster 分发 / 跨服务跳转 / 自我跳转。
//
// 测试以子进程方式启动服务二进制，不做同进程 import 走捷径。
package tests
